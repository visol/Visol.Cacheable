{
    "name": "visol/cacheable",
    "type": "neos-package",
    "description": "Allows caching the return value of Flow Framework methods through annotations",
    "license": "MIT",
    "require": {
        "neos/flow": "~5.3 || ~6.0 || dev-master"
    },
    "autoload": {
        "psr-4": {
            "Visol\\Cacheable\\": "Classes"
        }
    },
    "extra": {
        "neos": {
            "package-key": "Visol.Cacheable"
        }
    }
}
